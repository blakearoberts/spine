// Generated by CoffeeScript 1.12.4
(function() {
  var $, Firebase, Model, Spine, User,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Spine = this.Spine || require('spine');

  $ = Spine.$;

  Firebase = (function(superClass) {
    extend(Firebase, superClass);

    function Firebase(params) {
      Firebase.__super__.constructor.apply(this, arguments);
      this.bind('authStateChange', this.authStateChange);
    }

    Firebase.prototype.initFirebase = function(config) {
      var ctr, ui;
      ctr = this;
      firebase.initializeApp(config);
      ui = new firebaseui.auth.AuthUI(firebase.auth());
      ui.start('#firebaseui-auth-container', {
        signInOptions: [firebase.auth.GoogleAuthProvider.PROVIDER_ID, firebase.auth.FacebookAuthProvider.PROVIDER_ID, firebase.auth.TwitterAuthProvider.PROVIDER_ID, firebase.auth.GithubAuthProvider.PROVIDER_ID]
      });
      return firebase.auth().onAuthStateChanged((function(userParams) {
        ctr.log('firebase auth state change', userParams);
        if (userParams) {
          return userParams.getToken().then(function(accessToken) {
            userParams['accessToken'] = accessToken;
            return Spine.Firebase.User.signIn(userParams).done(function(data) {
              ctr.log('spine user signin', data);
              return ctr.trigger('authStateChange');
            });
          });
        }
      }), function(error) {
        return ctr.log('auth error', error);
      });
    };

    Firebase.prototype.authStateChange = function() {
      return console.log('auth state change triggered');
    };

    Firebase.prototype.onAuthStateChanged = function() {
      var ctr, deferred, promise;
      ctr = this;
      deferred = $.Deferred();
      promise = deferred.promise();
      firebase.auth().onAuthStateChanged((function(userParams) {
        ctr.log('firebase auth state change', userParams);
        if (userParams) {
          return userParams.getToken().then(function(accessToken) {
            userParams['accessToken'] = accessToken;
            return Spine.Firebase.User.signIn(userParams).done(function(data) {
              ctr.log('spine user signin', data);
              return deferred.resolve(data);
            });
          });
        } else {
          return deferred.reject();
        }
      }), function(error) {
        ctr.log('auth error', error);
        return deferred.reject();
      });
      return promise;
    };

    Firebase.signOut = function() {
      var deferred, promise, resolve;
      deferred = $.Deferred();
      promise = deferred.promise();
      resolve = function(data) {
        if (data) {
          return deferred.resolve(data);
        } else {
          return deferred.reject(data);
        }
      };
      firebase.auth().signOut().then(function() {
        if (Firebase.user) {
          delete Firebase.user;
        }
        return resolve();
      });
      return promise;
    };

    return Firebase;

  })(Spine.Controller);

  Model = {
    extended: function() {
      if (!firebase) {
        return;
      }
      this.change(this.saveFirebase);
      this.fetch(this.loadFirebase);
      if (this.listenToFirebase) {
        return this.startLoadListener();
      }
    },
    saveFirebase: function() {
      var result;
      result = JSON.parse(JSON.stringify(this));
      return firebase.database().ref(this.ref + result[0].id).set(result[0]);
    },
    startLoadListener: function() {
      return firebase.database().ref(this.ref + this.id).on('value', (function(_this) {
        return function(data) {
          return _this.refresh(data.val() || [], options);
        };
      })(this));
    },
    loadFirebase: function(options) {
      var deferred, promise;
      if (options == null) {
        options = {};
      }
      if (!options.hasOwnProperty('clear')) {
        options.clear = true;
      }
      deferred = $.Deferred();
      promise = deferred.promise();
      if (!firebase.auth().currentUser) {
        deferred.reject();
        return promise;
      }
      firebase.database().ref(this.ref).once('value').then((function(_this) {
        return function(data) {
          _this.refresh(data.val() || [], options);
          return deferred.resolve(data);
        };
      })(this));
      return promise;
    }
  };

  User = (function(superClass) {
    extend(User, superClass);

    function User() {
      return User.__super__.constructor.apply(this, arguments);
    }

    User.configure('User', 'displayName', 'email', 'accessToken');

    User.ref = '/users/';

    User.signIn = function(params) {
      var deferred, promise, userPath;
      userPath = params.email.replace(/([.@])/g, '-');
      deferred = $.Deferred();
      promise = deferred.promise();
      firebase.database().ref(User.ref + userPath).once('value').then(function(data) {
        if (data) {
          return deferred.resolve(data);
        } else {
          return deferred.reject(data);
        }
      });
      return promise;
    };

    return User;

  })(Spine.Model);

  Firebase.Model = Model;

  Firebase.User = User;

  Spine.Firebase = Firebase;

  if (typeof module !== "undefined" && module !== null) {
    module.exports = Firebase;
  }

}).call(this);
